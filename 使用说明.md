# 使用说明 · 实测记录与排错

> 项目说明、原理、编译烧录、日常使用、按键功能 —— 都在 **[README](README.md)**。
> 本文只记录 **本次实际烧录的实测结果** 和 **踩过的坑**，作为以后再动手时的参考。

---

## 一、实测结果

### 第一轮：原始固件

| 项目 | 结果 |
|---|---|
| 串口设备 | `COM4`（`VID_303A&PID_1001` = ESP32-C3 **原生 USB Serial/JTAG**，非 CH340） |
| 芯片识别 | `ESP32-C3 (QFN32) (revision v0.4)`，4MB 内置 Flash（XMC），Crystal 40MHz |
| 芯片 MAC | `48:f6:ee:15:8d:cc` |
| 编译 | ✅ Flash `852696 / 1310720`（**65.1%**），RAM `39468 / 327680`（12.0%） |
| 固件烧录 | ✅ 写入 `0x00010000`（含 bootloader `0x0` / partitions `0x8000` / boot_app0 `0xe000`） |
| 文件系统 | ✅ LittleFS 镜像写入 `0x00290000`，含 `/index.html`、`/style.css`、`/script.js`、`/favicon.ico` |
| 运行验证 | ✅ 串口抓到开机日志；WiFi 扫描**搜到了它广播的 `WIFI MANAGER` 热点** |

### 第二轮：加入「扫描附近 WiFi」

| 项目 | 结果 |
|---|---|
| 固件 | ✅ Flash `856160 / 1310720`（**65.3%**，比上轮只涨 3.5 KB） |
| 文件系统 | ✅ `littlefs.bin` 1441792 字节 → `0x00290000` |
| 开机日志 | ✅ `esp_wifi_set_mac: ESP_OK` + `AP created successfully` |
| 当时 NVS 里的配置 | SSID `<现场热点名>`、Password 空、MAC `<现场热点的BSSID>`（真实值在串口日志里） |

> NVS 这时**不是空的**（不像第一轮那三行 `NOT_FOUND`），说明中间已经有人配置过。
> 想清空重配就长按 BOOT > 1 秒；想临时改就 1.5 秒内连按三次进配置模式。

---

## 二、本机环境备忘

| 项目 | 值 |
|---|---|
| 串口 | `COM4` @ 115200（原生 USB CDC 忽略波特率，填什么都行） |
| PlatformIO CLI | **不在 PATH 里**，得用完整路径调用 |
| 板子型号 | `platformio.ini` 写的是 `board = esp32-c3-devkitm-1`（官方 DevKitM-1），实际是 **XIAO 外形的 ESP32C3 克隆板（CPU: ESP32C3FH4）**。两者芯片相同、Flash 都是 4MB，代码用到的 GPIO8 / GPIO9 定义也一致，可以直接用，不必改 |

```powershell
cd <项目目录>
$pio = "$env:USERPROFILE\.platformio\penv\Scripts\platformio.exe"

& $pio run                 # 只编译
& $pio run -t upload       # 烧固件
& $pio run -t uploadfs     # 烧文件系统（data/ → LittleFS）
& $pio device monitor      # 串口监视器
```

一分钟速查卡：

```
端口:        COM4  @ 115200
编译烧录:    pio run -t upload
烧网页:      pio run -t uploadfs
开机热点:    WIFI MANAGER  (无密码)
配置地址:    http://192.168.4.1
扫描选热点:  配置页下半部分点「开始扫描」→ 点「填入」
进配置模式:  1.5 秒内连按 BOOT 三次   (配置不丢)
清配置:      长按 BOOT 键 >1 秒       (配置丢失)
成功标志:    热点名/MAC 变成你填的值  (LED 常亮不一定可靠)
```

---

## 三、串口日志怎么看

日志走的是 ESP-IDF 的 **USB Serial/JTAG 二级控制台**（`CONFIG_ESP_CONSOLE_SECONDARY_USB_SERIAL_JTAG=y`），
**不需要**在代码里调用 `Serial.begin()`。

正常首次开机的日志长这样（实测原文）：

```
[   165][E][Preferences.cpp:483] getString(): nvs_get_str len fail: ssid NOT_FOUND
[   173][E][Preferences.cpp:483] getString(): nvs_get_str len fail: password NOT_FOUND
[   181][E][Preferences.cpp:483] getString(): nvs_get_str len fail: mac NOT_FOUND
[   193][D][WiFiGeneric.cpp:1040] _eventCallback(): Arduino Event: 0 - WIFI_READY
[   241][D][WiFiGeneric.cpp:1040] _eventCallback(): Arduino Event: 10 - AP_START
[   249][D][main.cpp:167] setup(): [MJOLNIR] No saved WiFi credentials. Creating default AP
```

已配置过的板子开机则是这样（第二轮实测原文，能看出当前存的是什么）：

```
[   229][D][main.cpp:52] convertMacStringToArray(): [MJOLNIR] Conversions successfully, MAC: ...
[   255][D][main.cpp:97] createAP(): [MJOLNIR] esp_wifi_set_mac: ESP_OK
[   306][D][main.cpp:103] createAP(): [MJOLNIR] esp_wifi_set_max_tx_power: ESP_OK
[   313][D][main.cpp:106] createAP(): [MJOLNIR] AP created successfully
[   319][D][main.cpp:107] createAP(): [MJOLNIR] #     SSID: ...
[   331][D][main.cpp:109] createAP(): [MJOLNIR] #      MAC: ...
```

> 那三行 `NOT_FOUND` 是**正常的**，表示还没保存过配置，不是错误。
> `ESP_LOGD` 能打出来，是因为 `platformio.ini` 里设了 `-D CORE_DEBUG_LEVEL=4`。

想自己抓一次开机日志（不依赖 VSCode）：

```powershell
powershell -ExecutionPolicy Bypass -File tools\boot-log.ps1 -Port COM4 -Seconds 12
```

脚本用经典 DTR/RTS 时序复位板子（DTR=GPIO0 保持高电平，避免误入下载模式），再连续读 12 秒串口。

---

## 四、踩过的坑 & 注意事项

### 4.1 硬件相关

**LED 指示可能不亮，属正常硬件差异。** 代码把 LED 接在 **GPIO8、低电平点亮**：

- 官方 Seeed XIAO ESP32C3 **没有用户 LED**（Arduino 变体里根本没定义 `LED_BUILTIN`）；
- 官方 ESP32-C3-DevKitM-1 的 GPIO8 接的是 **WS2812 彩灯**，不是普通 LED，`digitalWrite()` 驱动不会正常亮；
- 这块克隆板底部有一颗标 `L` 的灯，可能会亮。

不影响任何功能，只是"看灯判断状态"这个手段不可靠。**判断成功以「手机能不能搜到你填的那个 SSID」为准。**

**发射功率被调低了。** `createAP()` 里调了 `esp_wifi_set_max_tx_power(34)`（= 8.5 dBm，约为默认的一半以下），
这是原作者为**降低发热**做的处理。信号弱、连不上就把这个值调大
（在 `src/main.cpp` 里搜 `set_max_tx_power`，单位 0.25 dBm，`68` = 17 dBm）。

**`upload_speed = 3000000` 在原生 USB 板子上是被忽略的**（波特率无意义），实测正常。
⚠️ 但换成**带 CH340/CP2102 串口桥**的板子时，3000000 很可能烧不进去，建议降到 `921600` 或 `460800`。

### 4.2 提交配置后的行为

**提交成功后网页不会给响应。** 提交处理函数里保存完 NVS 之后**没有调用 `request->send(...)`**，
而 `createAP()` 中间又执行了 `server.end()` + `softAPdisconnect()`。结果是：

- 手机上的 `fetch()` 一直挂起直到超时，页面不会弹成功提示；
- 同时旧热点消失，手机自己就断开了。

第二轮改动在前端加了 `catch`，把这种情况提示成「已提交。热点正在重建…」，不再静默。
固件端**仍然没有回响应** —— 要彻底修好得把 `request->send(...)` 挪到 `createAP()` 之前，
那会动到热点重建流程，风险比收益大，暂时没动。

**配置合法性回退。** 开机时如果保存的 SSID 为空、或 MAC 长度不等于 17，会自动回到 `WIFI MANAGER` 热点，不会变砖。

**SSID 长度校验前后端不一致。** 网页表单 `maxlength="32"`，但固件只检查 `length() > 63` 才报错。
正常走表单不受影响；但用 `curl`/Postman 直接 POST 一个 33–63 字符的 SSID，固件会放行，
直到 `WiFi.softAP()` 才失败，而且报的是 `创建热点失败，请检查MAC地址是否正确` —— 会误导你去查 MAC。

### 4.3 「扫描附近 WiFi」的实现坑

1. **扫描必须异步。** 一次全信道扫描要 1.5–4 秒，同步版 `scanNetworks(false, ...)` 会把 ESPAsyncWebServer 的
   `async_tcp` 任务整个堵死，手机的 `fetch` 直接超时。所以拆成「`?start=1` 触发 + 轮询查询」两步。
2. **`scanComplete()` 的超时判据是 `max_ms_per_chan * 20`。** 核心默认 `max_ms_per_chan = 300`，即 6 秒；
   超过 6 秒它返回的是 `WIFI_SCAN_FAILED(-2)` 而不是"还在跑"。前端收到 `idle` 会**重新触发**一次扫描来兜底。
3. **扫描时 AP 会短暂抖动。** `WiFi.scanNetworks()` 内部会自己 `WiFi.enableSTA(true)` 把模式切成 AP_STA
   （所以不需要手动切模式，AP 也不会消失），但射频要在信道间跳，热点会有 1–2 秒不稳，手机可能提示"无互联网"。
   前端的轮询对失败做了容错，不会因此中断。
4. **`WiFi.scanDelete()` 必须调。** 每次扫描结果是一块 `new` 出来的堆内存，不收会持续泄漏。
5. **`serveStatic("/")` 不会吃掉 `/scan`。** 因为 `AsyncStaticWebHandler::canHandle()` 在文件不存在时返回 `false`
   （`WebHandlers.cpp:83`），请求会继续往后走到我们注册的路由。这一点确认过，才敢把 `/scan` 放在最后注册。
6. **只有 2.4GHz。** ESP32-C3 单频，5G 网络扫不到，配置页上已经把这条提示写死了。

---

## 五、源码改动说明（相对上游）

### 5.1 第一轮：连按 BOOT 三次进入配置模式

改动都在 `src/main.cpp`：

| 位置 | 改动 |
|---|---|
| 顶部 `#define` | 新增 `LONG_PRESS_MS` / `CLICK_MAX_MS` / `CLICK_DEBOUNCE_MS` / `CLICK_WINDOW_MS` / `CONFIG_CLICK_COUNT` / `CONFIG_MODE_TIMEOUT_MS` |
| 全局变量 | 新增 `configMode`、`configModeStartMs`、`clickCount`、`firstClickTime`、`lastClickTime` |
| `createAP()` | 成功建好自定义热点时置 `configMode = false`，表示已回到正常工作状态 |
| `enterConfigMode()` | **新增函数**：`server.end()` → `softAPdisconnect()` → 广播 `WIFI MANAGER` → `enableDNS = true` → `server.begin()` |
| 提交失败分支 | **兜底**：若 `createAP()` 已经断开热点导致设备失联，自动重播默认热点，保证还能连上去改配置 |
| `loop()` | 新增连击计数状态机 + 配置模式超时；轮询间隔从 `delay(50)` 收紧到 `delay(10)`，避免漏采短按 |

### 5.2 第二轮：扫描附近 WiFi

> ⚠️ **这一轮固件和网页都改了，两个都要烧：** `pio run -t upload` + `pio run -t uploadfs`

| 文件 | 改动 |
|---|---|
| `src/main.cpp` | 新增 `jsonEscape()`：转义 SSID 里的 `"` `\` 和控制字符，否则前端 `JSON.parse` 会炸 |
| `src/main.cpp` | 新增 `GET /scan` 路由：`?start=1` 触发 `WiFi.scanNetworks(true, true)` 异步扫描；不带参数则用 `WiFi.scanComplete()` 查状态，返回 `{"status":"scanning"\|"idle"\|"done","networks":[...]}` |
| `data/index.html` | 表单下方新增 `#scan_card`：扫描按钮 + 说明 + `#scan_status` / `#scan_result` 容器 |
| `data/script.js` | 新增扫描逻辑（触发 → 每 800ms 轮询，上限 20 秒）、结果渲染（按 RSSI 排序、按 BSSID 去重）、点「填入」写回表单；提交分支加 `catch` |
| `data/style.css` | `.content` 由固定 `height: 280px` 改为 `min-height` + `padding`（否则第二张卡片放不下）；新增扫描卡片/列表/按钮样式 |
| `tools/` | 新增 `boot-log.ps1`（抓开机日志）、`get-bssid.ps1`（查询热点 BSSID） |

**没有改动的行为**：长按清空配置、自定义热点的建立流程、MAC 校验规则，全部与原版一致。
