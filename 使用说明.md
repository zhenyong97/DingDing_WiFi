# DingDing_WiFi 烧录与使用说明

> 本文档记录 **2026 年本次实际烧录的完整过程与结果**，可作为以后重新烧录的操作手册。

---

## 一、本次已完成的工作（实测结果）

| 项目 | 结果 |
|---|---|
| 串口设备 | `COM4`（`VID_303A&PID_1001` = ESP32-C3 **原生 USB Serial/JTAG**，非 CH340） |
| 芯片识别 | `ESP32-C3 (QFN32) (revision v0.4)`，4MB 内置 Flash（XMC），Crystal 40MHz |
| 芯片 MAC | `48:f6:ee:15:8d:cc` |
| 编译 | ✅ 成功。Flash `852696 / 1310720` 字节（**65.1%**），RAM `39468 / 327680`（12.0%） |
| 固件烧录 | ✅ 写入 `0x00010000`（含 bootloader `0x0` / partitions `0x8000` / boot_app0 `0xe000`） |
| 文件系统烧录 | ✅ LittleFS 镜像写入 `0x00290000`，包含 `/index.html`、`/style.css`、`/script.js`、`/favicon.ico` |
| 运行验证 | ✅ 串口抓到了开机日志；且 WiFi 扫描**搜到了它广播的 `WIFI MANAGER` 热点** |

**结论：板子现在已经是可用的状态，插上电就在工作。**

### 补充：第二轮烧录（新增「扫描附近 WiFi」，2026 本次）

| 项目 | 结果 |
|---|---|
| 固件 | ✅ `pio run -t upload --upload-port COM4`，Flash `856160 / 1310720`（**65.3%**） |
| 文件系统 | ✅ `pio run -t uploadfs --upload-port COM4`，写入 `0x00290000`（`littlefs.bin` 1441792 字节） |
| 开机日志 | ✅ 抓到，`esp_wifi_set_mac: ESP_OK` + `AP created successfully` |
| **当时 NVS 里的配置** | SSID `<现场热点名>`、Password 空、MAC `<现场热点的BSSID>`（真实值在串口日志里） |

> 注意：这时 NVS **不是空的**（不像第一次烧录时那三行 `NOT_FOUND`），说明中间有人配置过。
> 想清空重配就长按 BOOT > 1 秒；想临时改就 1.5 秒内连按三次进配置模式。


---

## 二、怎么用它（手机端操作）

### 1. 连上它的热点
手机 WiFi 里找 **`WIFI MANAGER`**（无密码，开放网络），连上。

### 2. 打开配置页
正常情况下手机会**自动弹出**配置页（代码里跑了一个 DNS 劫持 + `onNotFound` 跳转的强制门户）。
如果没弹，手动用浏览器打开：

```
http://192.168.4.1
```

### 3. 填三个字段

| 字段 | 填什么 | 说明 |
|---|---|---|
| **SSID** | 公司 WiFi 的名字 | 必填，1–63 字符 |
| **Password** | 公司 WiFi 的密码 | 可为空（开放网络）；非空时必须 ≥8 位 |
| **MAC** | 公司 WiFi 热点的 **BSSID** | **必填，必须是 `aa:bb:cc:dd:ee:ff` 格式（严格 17 个字符）** |

> ⚠️ **MAC 填的是「公司那个路由/AP 的 BSSID」，不是手机的 MAC。**
> 代码依据：`src/main.cpp` 里 `esp_wifi_set_mac(WIFI_IF_AP, macArray)` —— 改的是 **AP 自己的 MAC**，
> 也就是 ESP32 广播出去、手机看到的那个热点的 BSSID。手机自己的 MAC 全程不参与。
>
> 🎯 **最省事的办法：用配置页自带的「📡 扫描附近 WiFi」**（见下一节）。
> 人站在公司里、手机连着板子的热点，点一下「开始扫描」，列表里直接列出附近每个热点的
> **名字 + MAC(BSSID) + 信道 + 信号**，点「填入」就自动填好两个字段 —— 不需要装 App、不需要管理员权限。
>
> 如果板子扫不到（说明那个 WiFi 只有 5GHz，板子射频是 2.4GHz 单频），再走下面的办法：
> - Android 装 *WiFiAnalyzer* 之类的 App，能同时看到 2.4G 和 5G 的 BSSID；
> - **问公司钉钉考勤管理员**：考勤后台 → WiFi 打卡设置里就登记着 SSID + MAC，最准，零猜测；
> - 本仓库脚本（Windows，自动提权）：`powershell -ExecutionPolicy Bypass -File tools\get-bssid.ps1`；
> - 或公司路由器后台「无线设置」里的 `BSSID / 2.4G MAC`。
>
> ⚠️ iPhone 设置里那个「Wi-Fi 地址」是**手机的** MAC，不是 BSSID，别抄错。

### 3.1 「📡 扫描附近 WiFi」怎么用

配置页下半部分有一张独立的卡片：

1. 点 **「开始扫描」** → 等 2–6 秒（扫描期间热点会短暂卡顿一下，手机可能提示"无互联网"，属正常）；
2. 列出附近所有 **2.4GHz** 热点，按信号从强到弱排，每行是
   `名字 / MAC(BSSID) · 信道 · 信号 · 加密或开放`；
3. 点某一行的 **「填入」** → 名字和 MAC 自动填进上面的表单，再点 **👌** 提交。

> 隐藏网络（没名字的热点）会显示成「（隐藏网络）」，按钮变成「只填MAC」—— 只填 MAC，名字要自己输。
> 扫描实现是**异步**的（`GET /scan?start=1` 触发 + 轮询 `GET /scan`），不会把 web 服务堵住；
> 前端最多轮询 20 秒，超时会提示重连热点后再试。

填完点 **👌** 提交。

### 4. 判断成功没有

- **成功**：板子上的 LED（GPIO8）**常亮**，热点名字变成你填的 SSID、BSSID 也变成了你填的 MAC —— 手机需要重新去连这个新热点。
- **失败**：页面会返回错误文字，例如 `创建热点失败，请检查MAC地址是否正确` / `MAC length error`。

### 5. 之后的使用

配置保存在 NVS（Preferences）里，**断电不丢**。以后每次上电会自动用保存的 SSID + MAC 重建热点，不需要再配。

### 6. 公司换了 WiFi 怎么办（不需要重新烧录固件）

配置存在 NVS 分区，和固件、网页文件互不相干 —— **换 WiFi 只要重新配置，永远不用重新烧固件**。三条路：

| 方式 | 操作 | 配置会不会丢 |
|---|---|---|
| **A. 三击进配置模式**（推荐） | 1.5 秒内连按 BOOT 三次 → 热点变回 `WIFI MANAGER` → 手机重填三个字段提交 | ❌ 不丢 |
| **B. 连旧热点直接改** | 手机连上它现在广播的那个（旧的）公司 WiFi 名热点，浏览器手动开 `http://192.168.4.1` 改 | ❌ 不丢 |
| **C. 长按清空重配** | 长按 BOOT > 1 秒清空 → 重新填 | ✅ 丢失 |

**为什么优先用 A**：方式 B 有个会卡死的场景 —— 如果公司连密码一起换了、手机忘了那个网络、或者手机嫌「无互联网」拒绝连接，你就连不上旧热点，也就打不开配置页，只能走路 C 把配置全丢掉。A 绕开了这个依赖。

**配置模式的保护机制：**

- 已保存的配置**原封不动**留在 NVS 里，进配置模式不会动它；
- 不想改了：直接断电重启，自动回到原来那个热点；
- **忘了退出也没关系**：配置模式最长保持 5 分钟，超时自动重启回到已保存的配置，不会一直停在 `WIFI MANAGER` 上导致打卡失败。

---

## 三、按键功能

| 按键 | 位置 | 操作 | 功能 |
|---|---|---|---|
| **BOOT** | 板子底部右侧（GPIO9） | **1.5 秒内连按 3 次** | 进入**配置模式**：临时变回 `WIFI MANAGER` 热点 + 恢复强制门户，**已保存的配置保持不变** |
| **BOOT** | 同上 | **长按超过 1 秒再松开** | 清除保存的 WiFi 配置并自动重启，回到默认热点（**配置丢失**） |
| **RST** | 板子底部左侧 | 按一下 | 纯硬件复位，不影响保存的配置 |

> 两者都是**松手那一刻**才生效。注意：**长按和短按必须区分开** —— 按 0.8 秒以内算"一次短按"，超过 1 秒才算长按。
>
> ⚠️ **千万不要在上电/复位的时候按着 BOOT**。GPIO9 是 ESP32-C3 的 BOOT 选通脚，上电时被按住会让芯片进入 **ROM 下载模式**，固件根本不运行（现象：热点消失、串口无日志，看起来像板子坏了）。拔掉 USB、松开按键、重新插上即可恢复，没有后遗症。

---

## 四、串口日志怎么看

- 端口 **`COM4`**，波特率 **115200**（原生 USB CDC 其实忽略波特率，填什么都行）
- 日志走的是 ESP-IDF 的 **USB Serial/JTAG 二级控制台**（`CONFIG_ESP_CONSOLE_SECONDARY_USB_SERIAL_JTAG=y`），**不需要**在代码里调用 `Serial.begin()`。

正常首次开机的日志长这样（本次实测原文）：

```
[   165][E][Preferences.cpp:483] getString(): nvs_get_str len fail: ssid NOT_FOUND
[   173][E][Preferences.cpp:483] getString(): nvs_get_str len fail: password NOT_FOUND
[   181][E][Preferences.cpp:483] getString(): nvs_get_str len fail: mac NOT_FOUND
[   193][D][WiFiGeneric.cpp:1040] _eventCallback(): Arduino Event: 0 - WIFI_READY
[   241][D][WiFiGeneric.cpp:1040] _eventCallback(): Arduino Event: 10 - AP_START
[   249][D][main.cpp:167] setup(): [MJOLNIR] No saved WiFi credentials. Creating default AP
```

> 那三行 `NOT_FOUND` 是**正常的**，表示还没保存过配置，不是错误。

想自己抓一次开机日志（不依赖 VSCode），可以用本仓库附带的脚本：

```powershell
pwsh -File tools\boot-log.ps1 -Port COM4 -Seconds 12
```

---

## 五、以后自己在 VSCode / PlatformIO 里重烧

### 方式 A：图形界面（推荐给你）

打开 VSCode → 左侧 **PlatformIO 图标（蚂蚁头）** → `PROJECT TASKS` → `esp32-c3-devkitm-1` → `General`：

| 任务 | 作用 | 什么时候用 |
|---|---|---|
| **Build** | 只编译 | 想先看有没有语法错误 |
| **Upload** | 编译 + 烧录**固件** | 改了 `src/main.cpp` |
| **Upload Filesystem Image** | 只烧录 **data/ 目录**（LittleFS） | 只改了 `data/index.html` / `style.css` / `script.js` |
| **Monitor** | 打开串口监视器 | 看日志 |

> ⚠️ **最容易踩的坑**：`Monitor` 开着的时候点 `Upload` 会失败（串口被占用，报 `could not open port`）。**烧录前先把监视器关掉。**

### 方式 B：命令行（等效，本次就是这么做的）

```powershell
cd C:\Users\LZyon\Desktop\DingDing_WiFi

# PlatformIO 的 CLI 不在 PATH 里，用完整路径调用
$pio = "$env:USERPROFILE\.platformio\penv\Scripts\platformio.exe"

& $pio run                 # 编译
& $pio run -t upload       # 烧录固件
& $pio run -t uploadfs     # 烧录文件系统（data/ 目录）
& $pio device monitor      # 串口监视器（Ctrl+C 退出）
```

**改动 → 该烧哪个？**

```
改了 src/main.cpp        →  只需要 pio run -t upload
改了 data/ 里的网页文件  →  只需要 pio run -t uploadfs
两者都改了               →  两个都跑一遍
```

---

## 六、本次踩到的坑 & 注意事项

1. **板子型号和 `platformio.ini` 不一致，但不影响使用。**
   `platformio.ini` 第 13 行写的是 `board = esp32-c3-devkitm-1`（官方 DevKitM-1），你的板子是 **XIAO 外形的 ESP32C3 克隆板（CPU: ESP32C3FH4）**。
   两者芯片相同、Flash 都是 4MB，代码用到的 **GPIO8 / GPIO9 引脚定义也一致**，所以可以直接用，不必改。

2. **`upload_speed = 3000000` 在你这块板上是被忽略的。**
   因为走的是原生 USB-Serial/JTAG（波特率无意义），实测正常。
   ⚠️ 但如果以后换成**带 CH340/CP2102 串口桥**的 ESP32-C3 板子，3000000 很可能烧不进去，建议把 `upload_speed` 降到 `921600` 或 `460800`。

3. **LED 指示可能不亮，属正常硬件差异。**
   代码把 LED 接在 **GPIO8、低电平点亮**。
   - 官方 Seeed XIAO ESP32C3 **没有用户 LED**（Arduino 变体里根本没定义 `LED_BUILTIN`）；
   - 官方 ESP32-C3-DevKitM-1 的 GPIO8 接的是 **WS2812 彩灯**，不是普通 LED，用 `digitalWrite()` 驱动不会正常亮；
   - 你这块克隆板图片底部有一颗标 `L` 的灯，可能会亮。

   **不影响任何功能**，只是"看灯判断状态"这个手段不一定可靠。判断成功还是以「手机能不能搜到你填的那个 SSID」为准。

4. **提交成功后网页不会给提示（原代码的小 bug）。**
   `src/main.cpp` 第 128–139 行：提交成功后只保存了配置，**没有调用 `request->send(...)` 回响应**，而且中间还执行了 `server.end()` + `softAPdisconnect()`。结果是：
   - 手机上的 `fetch()` 会一直挂起直到超时，页面**不会弹出成功提示**；
   - 同时旧热点消失，手机自己就断开了。

   所以**别等网页提示**，直接看板子热点名字是不是变了 / 重新扫一下 WiFi 确认。
   （第二轮改动后前端加了 `catch`：提交导致掉线时不再静默，会提示"已提交。热点正在重建…"。
   固件端仍然没有回响应 —— 要彻底修好得把 `request->send(...)` 提到 `createAP()` 之前，那个改动更靠里，暂时没动。）

5. **配置合法性回退。** 开机时如果保存的 SSID 为空或 MAC 长度不等于 17，会自动回退到默认的 `WIFI MANAGER` 热点，不会变砖。

6. **发射功率被调低了。** 创建自定义热点时代码调用了 `esp_wifi_set_max_tx_power(34)`（= 8.5 dBm，约为默认的一半以下），这是原作者为**降低发热**做的处理。如果发现信号弱、连不上，可以把 `src/main.cpp` 第 60 行的 `34` 调大（单位 0.25 dBm，如 `68` = 17 dBm）。

7. **手机连上这个热点后没有外网。** 它是纯 AP，没有 STA 上行。打卡时让手机连着它即可（或开移动数据）。

---

## 七、一分钟速查卡

```
端口:        COM4  @ 115200
编译烧录:    pio run -t upload
烧网页:      pio run -t uploadfs
开机热点:    WIFI MANAGER  (无密码)
配置地址:    http://192.168.4.1
扫描选热点:  配置页下半部分点「开始扫描」→ 点「填入」
进配置模式:  1.5 秒内连按 BOOT 三次   (配置不丢)
清配置:      长按 BOOT 键 >1 秒       (配置丢失)
成功标志:    热点名/MAC 变成你填的值  (LED 常亮不一定可靠)
```

---

## 八、源码改动说明

### 8.1 第一轮：连按 BOOT 三次进入配置模式

在**原版**基础上加了功能：**连按 BOOT 三次进入配置模式**，并顺手做了两处健壮性兜底。改动都在 `src/main.cpp`：

| 位置 | 改动 |
|---|---|
| 顶部 `#define` | 新增 `LONG_PRESS_MS` / `CLICK_MAX_MS` / `CLICK_DEBOUNCE_MS` / `CLICK_WINDOW_MS` / `CONFIG_CLICK_COUNT` / `CONFIG_MODE_TIMEOUT_MS` |
| 全局变量 | 新增 `configMode`、`configModeStartMs`、`clickCount`、`firstClickTime`、`lastClickTime` |
| `createAP()` | 成功建好自定义热点时置 `configMode = false`，表示已回到正常工作状态 |
| `enterConfigMode()` | **新增函数**：`server.end()` → `softAPdisconnect()` → 广播 `WIFI MANAGER` → `enableDNS = true` → `server.begin()` |
| 提交失败分支 | **兜底**：若 `createAP()` 已经断开热点导致设备失联，自动重播默认热点，保证还能连上去改配置 |
| `loop()` | 新增连击计数状态机 + 配置模式超时；轮询间隔从 `delay(50)` 收紧到 `delay(10)`，避免漏采短按 |

### 8.2 第二轮：扫描附近 WiFi（解决"现场拿不到 BSSID"）

**背景**：填 MAC 需要知道公司热点的 BSSID，但 Android 10 以后很多 ROM 的 WiFi 详情页不显示 BSSID，
iPhone 干脆不给，Windows 的 `netsh` 又要管理员+位置服务。做这个功能后，**现场只要有手机就行**。

> ⚠️ **这一轮固件和网页都改了，两个都要烧：**
> ```powershell
> & $pio run -t upload      # 固件（新增 /scan 接口）
> & $pio run -t uploadfs    # 网页（新增扫描卡片）
> ```

| 文件 | 改动 |
|---|---|
| `src/main.cpp` | 新增 `jsonEscape()`：把 SSID 里的 `"` `\` 和控制字符转义，否则前端 `JSON.parse` 会炸 |
| `src/main.cpp` | 新增 `GET /scan` 路由：`?start=1` 触发 `WiFi.scanNetworks(true, true)` **异步**扫描；不带参数则用 `WiFi.scanComplete()` 轮询，返回 `{"status":"scanning"\|"idle"\|"done", "networks":[...]}` |
| `data/index.html` | 表单下方新增 `#scan_card`：扫描按钮 + 说明 + `#scan_status` / `#scan_result` 容器 |
| `data/script.js` | 新增扫描逻辑（触发 → 每 800ms 轮询，上限 20 秒）、结果渲染（按 RSSI 排序、按 BSSID 去重）、点「填入」写回表单；提交失败分支加了 `catch`，把"热点重建导致掉线"提示成正向文案 |
| `data/style.css` | `.content` 由固定 `height: 280px` 改为 `min-height` + `padding`（否则第二张卡片放不下）；新增扫描卡片/列表/按钮样式 |

**几个实现上的坑，记一下：**

1. **扫描必须异步。** 一次全信道扫描要 1.5–4 秒，同步版 `scanNetworks(false, ...)` 会把 ESPAsyncWebServer 的
   `async_tcp` 任务整个堵死，手机的 `fetch` 直接超时。所以拆成"触发 + 轮询"两步。
2. **`scanComplete()` 的超时判据是 `max_ms_per_chan * 20`。** 核心默认 `max_ms_per_chan = 300`，
   即 6 秒；超过 6 秒它会返回 `WIFI_SCAN_FAILED(-2)` 而不是"还在跑"。前端收到 `idle` 会**重新触发**一次扫描来兜底。
3. **扫描时 AP 会短暂抖动。** `WiFi.scanNetworks()` 内部会 `WiFi.enableSTA(true)` 把模式切成 AP_STA，
   射频要在信道间跳，热点会有 1–2 秒不稳，手机可能提示"无互联网"。前端的轮询对失败做了容错，不会因此中断。
4. **`WiFi.scanDelete()` 必须调。** 每次扫描结果是一块 `new` 出来的堆内存，不收会持续泄漏。
5. **`serveStatic("/")` 不会吃掉 `/scan`。** 因为 `AsyncStaticWebHandler::canHandle()` 在文件不存在时返回 `false`
   （见 `WebHandlers.cpp:83`），请求会继续往后走到我们注册的路由。这一点确认过才敢把 `/scan` 放在最后注册。
6. **只有 2.4GHz。** ESP32-C3 单频，5G 网络扫不到，页面上已经把这条提示写死了。

**没有改动的行为**：长按清空配置、自定义热点的建立流程、MAC 校验规则，全部与原版一致。
